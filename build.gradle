apply plugin: 'java'
sourceSets {
    main {
        java {
            srcDir 'src'
        }
        resources {
            srcDir 'webcontent'
        }
    }
}

dependencies {
    compile fileTree(dir: 'WEB-INF/lib', include: ['*.jar'])
    compile fileTree(dir: '/usr/local/Cellar/tomcat/8.0.21/libexec/lib', include: ['*.jar'])
}